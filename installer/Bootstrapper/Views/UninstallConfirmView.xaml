<UserControl x:Class="reShutCLI.Bootstrapper.Views.UninstallConfirmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:theme="clr-namespace:reShutCLI.Bootstrapper.Theme">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Ellipse Style="{StaticResource PageGlow}" Margin="0,-24,0,0" />

            <StackPanel VerticalAlignment="Center" Margin="44,0">
                <Border Style="{StaticResource IconTile}" theme:Reveal.Delay="0">
                    <Image Source="{x:Static theme:AppIcon.Large}" Width="30" Height="30" RenderOptions.BitmapScalingMode="HighQuality" />
                </Border>

                <TextBlock Text="Uninstall reShut CLI" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,16,0,0"
                           theme:Reveal.Delay="0.06" />
                <TextBlock Text="This will remove reShut CLI and its Start Menu shortcuts from this computer."
                           FontSize="12.5" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center"
                           Margin="0,8,0,0" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="360"
                           theme:Reveal.Delay="0.12" />

                <CheckBox x:Name="RemoveSettingsCheck" Content="Also remove settings from the registry"
                          HorizontalAlignment="Center" Margin="0,28,0,0" theme:Reveal.Delay="0.2" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource FooterBar}" theme:Reveal.Delay="0.27" theme:Reveal.Rise="0">
            <Grid Margin="24,14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="OnCancelClick" />
                    <Button Content="Uninstall" Style="{StaticResource AccentButton}" Width="110" Margin="10,0,0,0" Click="OnUninstallClick" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
