<UserControl x:Class="reShutCLI.Bootstrapper.Views.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:theme="clr-namespace:reShutCLI.Bootstrapper.Theme">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center" Margin="56,0">
            <Border x:Name="Badge" Width="64" Height="64" CornerRadius="32" Background="{StaticResource DangerBrush}"
                    HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <TranslateTransform X="0" />
                </Border.RenderTransform>
                <Border.Effect>
                    <DropShadowEffect Color="{StaticResource DangerColor}" BlurRadius="26" ShadowDepth="0" Opacity="0.45" />
                </Border.Effect>
                <Path Data="M13,4 L15,4 L15,17 L13,17 Z M13,21 L15,21 L15,23 L13,23 Z" Fill="White" Width="16" Height="24" Stretch="Uniform" />
            </Border>

            <TextBlock Text="Something went wrong" FontSize="19" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,18,0,0"
                       theme:Reveal.Delay="0.14" />
            <TextBlock x:Name="MessageText" FontSize="12" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center"
                       Margin="0,8,0,0" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="380"
                       theme:Reveal.Delay="0.2" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource FooterBar}" theme:Reveal.Delay="0.27" theme:Reveal.Rise="0">
            <Grid Margin="24,14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Close" Style="{StaticResource SecondaryButton}" Click="OnCloseClick" />
                    <Button x:Name="RetryButton" Content="Retry" Style="{StaticResource AccentButton}" Width="110" Margin="10,0,0,0" Click="OnRetryClick" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="Badge" Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:0.25" />
                        <!-- A short, decaying shake - enough to register as "stop and read this". -->
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Badge"
                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                       BeginTime="0:0:0.18">
                            <LinearDoubleKeyFrame Value="0" KeyTime="0:0:0" />
                            <LinearDoubleKeyFrame Value="-7" KeyTime="0:0:0.06" />
                            <LinearDoubleKeyFrame Value="7" KeyTime="0:0:0.16" />
                            <LinearDoubleKeyFrame Value="-4" KeyTime="0:0:0.26" />
                            <LinearDoubleKeyFrame Value="3" KeyTime="0:0:0.34" />
                            <LinearDoubleKeyFrame Value="0" KeyTime="0:0:0.42" />
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>
    </Grid>
</UserControl>
